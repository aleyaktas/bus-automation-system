import { NavbarStyleProps } from "./Navbar.types";

const styleFn: NavbarStyleProps = () => ({
  container: {
    height: "7vh",
    background: "white",
  },
  navIcon: {
    fontSize: "2.5rem",
  },
});

export default styleFn;
